"""End-to-end (Playwright) test harness for the juice web UI.

Modules here are run as scripts (``python -m tests.e2e.serve``), not collected by
pytest — they carry no ``test_`` prefix and must stay import-side-effect-free.
"""
