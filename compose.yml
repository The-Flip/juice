services:
  recorder:
    build: .
    environment:
      - KASA_USERNAME
      - KASA_PASSWORD
      - FLIPFIX_API_URL
      - FLIPFIX_API_KEY
    volumes:
      - ./data:/data
    restart: unless-stopped
